// checks if the array includes a certain value/objects
// returns true/false
const num = [1, 2, 3, 6, 2378, 27];
const includesThree = num.includes(3);
const includesHello = num.includes('hello');
const includesFour = num.includes(4);
console.log(includesThree, includesHello, includesFour);