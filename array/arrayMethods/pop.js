// similar to shift, but for the END
// also changes the length fo the array
const array = [10, 'student', 'cake', -78, 'zebra'];
console.log('before:', array);
const lastItem = array.pop();
console.log('after:', array);
console.log(lastItem);