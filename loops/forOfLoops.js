// -iterate over the values of an iterable objects
// iterae means to be able to parse through with numbers

let soupArray = [ 'potato', 'carrot', 'chili', 'stew'];
for(soup of soupArray) {
    console.log(soup);
}

